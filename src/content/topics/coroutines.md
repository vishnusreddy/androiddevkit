---
title: Coroutines & Flow
description: Structured concurrency, scopes, dispatchers, cancellation, and reactive streams with Flow.
category: Kotlin
order: 10
icon: "⟳"
---

Coroutines are the single most-asked Android topic. Expect questions on
structured concurrency, the difference between `launch` and `async`, dispatcher
choice, cancellation, and how `Flow` compares to `LiveData` and `RxJava`.
