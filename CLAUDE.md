# Project instructions

Follow the repository-wide guidance in `AGENTS.md`, especially the browser and
editorial-quality checklists. For every UI change, explicitly verify
Safari/WebKit as well as Chromium, including native form controls and persistent
selected/disabled states. For interview content, require precise failure modes,
plausible distractors, meaningful caveats, and a passing `npm run content:check`.
